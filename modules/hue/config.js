/**
 * @module
 * This module defines the settings that are needed for the Hue skill
 *
 * hueUserId   : The Hue user you have created on your bridge.
 * hueBridgeIP : The IP address of the bridge
 */

const config = {
  hueUserId: '',
  hueBridgeIP: '',
};

module.exports = config;
